name: Android CI From Zip

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Unzip Project Files
      run: |
        sudo apt-get install unzip
        unzip project.zip -d extracted_project
        
        # मुख्य फ़ोल्डर का पता लगाना जहाँ build.gradle छिपी है
        PROJECT_DIR=$(find extracted_project -name "build.gradle" -o -name "build.gradle.kts" | head -n 1 | xargs dirname)
        echo "PROJECT_PATH=$PROJECT_DIR" >> $GITHUB_ENV
        echo "प्रोजेक्ट इस फ़ोल्डर में मिला: $PROJECT_DIR"

    - name: set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Generate Gradle Wrapper Inside Project
      run: |
        cd ${{ env.PROJECT_PATH }}
        gradle wrapper --gradle-version 8.0
        chmod +x gradlew

    - name: Build APK with Gradle
      run: |
        cd ${{ env.PROJECT_PATH }}
        ./gradlew assembleDebug --no-daemon

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: my-app-apk
        path: |
          **/build/outputs/apk/debug/*.apk
          
